class Player {
  constructor(id) {
    this.id = id;
    this.playerDeck = []; //new deck
    this.wins = 0;
    // console.table('Player=', this);
  }

  playCard() {
    // TBD
  }

  saveWinsToStorage() {
    //save wins to storage
  }

}